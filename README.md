<img width="1306" height="570" alt="image" src="https://github.com/user-attachments/assets/00c470f7-7b1a-49fc-aa4e-fd6c6c8e4066" />

Мы стремимся разработать алгоритм оптимизации обработки поля трактором с прицепным оборудованием.
Прицепное оборудование может отличаться расстоянием до трактора (длиной сцепки), шириной захвата поля.
Поле необходимо обрабатывать только поперек склона. ЦМР и направление склона нужно рассчитывать в Google Earth Engine.

Конструктивная ширина захвата плуга ППРО-12-01: 5,04 м.
Габаритные размеры плуга в транспортном положении (мм): длина — 21100, ширина — 3000, высота — 3600.
Минимальный радиус поворота трактора К-700: 7200 мм


🔑 Ключевые улучшения алгоритма:
1. Независимость от вершин полигона
Гоны строятся на основе равномерной сетки, покрывающей весь ограничивающий прямоугольник

Линии обрезаются полигоном поля после генерации, а не привязываются к его вершинам

2. Учет отверстий в поле
Используется intersection() с полигоном, который может содержать отверстия (interiors)

Гоны автоматически не генерируются внутри вырезанных областей

3. Запрет пересечений
Функция check_intersections() проверяет, что буферы вокруг гонов не пересекаются

Допускается только краевое перекрытие (параметр edge_overlap)

4. Объединение сегментов
Короткие сегменты, лежащие на одной линии, объединяются в более длинные гоны

Учитывается максимальный разрыв между сегментами

5. Улучшенная оптимизация
Учитывается штраф за повороты в функции стоимости

Разные стратегии начальной популяции

Турнирный отбор с элитизмом

📊 Ожидаемые результаты:
Количество гонов сократится до разумного значения (100-200 вместо 574)

Длина рабочего хода увеличится (гоны будут длиннее и менее фрагментированы)

Эффективность повысится до 70-80% (сейчас 49.2%)

Гоны не будут пересекаться, за исключением преднамеренного краевого перекрытия

⚙️ Настройка параметров:
python
# Основные настраиваемые параметры:
'edge_overlap': 0.1,          # Перекрытие на краях (10%)
'min_segment_length': 3.0,    # Минимальная длина сегмента гона
'connection_method': 'shortest', # Метод соединения гонов
🎯 Следующие шаги после запуска:
Проверьте файл coverage_lines.geojson в QGIS - гоны должны равномерно покрывать поле

Убедитесь, что отверстия не заполнены

Проанализируйте траекторию на карте optimized_track_map.html

При необходимости отрегулируйте параметры:

Увеличьте edge_overlap для гарантированного покрытия

Уменьшите min_segment_length для лучшего покрытия сложных форм

Измените population_size/max_generations для более тщательной оптимизации

Этот алгоритм решает основную проблему привязки к вершинам полигона и должен дать значительно лучшие результаты, чем предыдущая версия.
